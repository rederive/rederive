{
  "name": "sirpm",
  "version": "0.1.0",
  "description": "The trust-nothing package manager. SIR packages ship a CONTRACT (spec + held-out oracle), not trusted bytes — verify the shipped code against it, or rebuild it locally from the spec.",
  "type": "module",
  "bin": { "sirpm": "cli/sirpm.mts" },
  "workspaces": ["packages/*"],
  "scripts": {
    "sirpm": "tsx cli/sirpm.mts",
    "check": "tsx cli/sirpm.mts check packages/colors",
    "vis": "tsx cli/sirpm.mts vis packages/colors"
  },
  "devDependencies": { "tsx": "^4.19.0" },
  "license": "MIT"
}
