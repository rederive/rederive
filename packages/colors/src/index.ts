// @sirpm/colors — verified-recompose of the colors terminal-styling library (zero-dep core).
// Each export is a quorum-verified, behavior-locked unit; see sir.manifest.json for provenance + proof.
export { strip } from './strip';
export { strip as stripColors } from './strip';
